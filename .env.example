# Optional runtime configuration for local development

# Admin credentials for #admin login screen.
# Defaults are used if these are not provided.
VITE_ADMIN_EMAIL=admin@agtechsummit.in
VITE_ADMIN_PASSWORD=admin123

# Geo IP endpoint used to auto-detect currency on registration page.
# Must return JSON containing country_name.
VITE_IP_LOOKUP_URL=https://ipapi.co/json/
